# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting

# An example controller showing a sample home page
# GET     /                           controllers.HomeController.index()

# Map static resources from the /public folder to the /assets URL path

GET /ws controllers.BasicController.socket

GET /logout controllers.BasicController.logout()

POST /dump controllers.BasicController.thing()

+nocsrf
POST /googleAuth	controllers.BasicController.signinVerification()

+nocsrf
POST /testDump	controllers.BasicController.testRecieve()

GET /getUserInfo controllers.BasicController.giveUserInfo()

GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

GET /yt controllers.Default.redirect(to = "https://youtube.com/@burgermanjoe")

GET /test1 controllers.BasicController.returnJson()

GET / controllers.BasicController.index()

GET /home controllers.BasicController.notDone()

GET /:name controllers.BasicController.fullRedirect(name: String)

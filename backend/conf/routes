# Routes

# GET /createLiveCampaign controllers.CampaignController.

GET /ws controllers.CampaignController.echo

GET /logout controllers.DbController.logout()

+nocsrf
POST /googleAuth	controllers.DbController.signinVerification()

GET /getUserInfo controllers.DbController.giveUserInfo()
